node_modules/
build/
dist/
daemon/dist/
coverage/
.DS_Store
*.log
logs/*.log
.env*
!.env.example
supabase/.temp/
scripts/ls-export.json

# Runtime state — machine-local, regenerated by the daemon on each run
data/daemon-state.json
data/universe-cache.json

# IDE
.idea/

# analysis scratch caches
bars_cache.json
daily_cache.json

# trade-data backups (timestamped, machine-local audit copies — never commit)
data/*.bak-*
data/*.pre-restate-*
data/*.pre-reconcile-*
data/*.backup
